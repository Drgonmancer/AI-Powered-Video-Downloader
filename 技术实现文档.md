# AI万能视频下载器 - 技术实现方案（详细版）

> 本文档是代码级技术设计，每个模块都包含核心伪代码/接口定义，开发时直接按此实现。

---

## 一、后端架构详解

### 1.1 main.py 整体结构

main.py 是后端唯一入口文件，包含：FastAPI应用实例、全局配置、任务管理器、所有API路由、WebSocket。

```
main.py 文件结构（约300行）：
├── 导入区（标准库 + 第三方）
├── 全局变量
│   ├── config: dict              # 运行时配置
│   ├── tasks: Dict[str, Task]    # 任务字典 {task_id: Task}
│   ├── ws_connections: set       # WebSocket连接集合
│   └── semaphore: Semaphore      # 并发控制信号量
├── 数据模型（Pydantic）
│   ├── ParseRequest / ParseResponse
│   ├── DownloadRequest / DownloadResponse
│   ├── BatchRequest
│   └── SettingsUpdate
├── 核心类
│   ├── Task                     # 下载任务数据类
│   └── ConnectionManager        # WebSocket连接管理
├── 辅助函数
│   ├── _pick_best_format()      # 智能选格式
│   ├── _normalize_info()        # yt-dlp原始数据标准化
│   └── _get_platform()          # 从URL提取平台标识
├── API路由（8个HTTP端点 + 1个WS端点）
└── 启动入口（if __name__ == "__main__"）
```

### 1.2 全局数据结构

```python
# ===== 配置 =====
config = {
    "download_path": os.path.join(os.getcwd(), "downloads"),
    "output_format": "mp4",
    "default_quality": "best",
    "max_concurrent": 3,
    "speed_limit": 0,            # 0 = 不限速，单位 bytes/s
}

# ===== 任务存储 =====
tasks: Dict[str, 'Task'] = {}

# ===== WebSocket =====
class ConnectionManager:
    def __init__(self):
        self.active: Set[WebSocket] = set()

    async def connect(self, ws: WebSocket):
        await ws.accept()
        self.active.add(ws)

    def disconnect(self, ws: WebSocket):
        self.active.discard(ws)

    async def broadcast(self, message: dict):
        for ws in list(self.active):
            try:
                await ws.send_json(message)
            except:
                self.active.discard(ws)

ws_manager = ConnectionManager()

# ===== 并发控制 =====
semaphore = asyncio.Semaphore(config["max_concurrent"])
```

### 1.3 Task 数据模型

```python
import uuid
import time
from dataclasses import dataclass, field
from typing import Optional
from enum import Enum

class TaskStatus(str, Enum):
    QUEUED = "queued"
    DOWNLOADING = "downloading"
    PAUSED = "paused"
    MERGING = "merging"
    COMPLETED = "completed"
    FAILED = "failed"

@dataclass
class Task:
    id: str = field(default_factory=lambda: str(uuid.uuid4())[:8])
    url: str = ""
    title: str = ""
    platform: str = ""
    thumbnail: str = ""
    duration: int = 0
    format_id: str = ""
    output_format: str = "mp4"
    filepath: str = ""
    filesize: int = 0
    status: TaskStatus = TaskStatus.QUEUED
    progress: float = 0.0
    speed: str = ""
    eta: str = ""
    error_message: str = ""
    created_at: float = field(default_factory=time.time)
    started_at: Optional[float] = None
    completed_at: Optional[float] = None
    # 内部运行时字段（不暴露给前端）
    _process: Optional[subprocess.Popen] = None
    _ydl_instance = None       # YoutubeDL实例引用（用于暂停）

    def to_dict(self) -> dict:
        return {
            "id": self.id,
            "url": self.url,
            "title": self.title,
            "platform": self.platform,
            "thumbnail": self.thumbnail,
            "duration": self.duration,
            "format_id": self.format_id,
            "output_format": self.output_format,
            "filepath": self.filepath,
            "filesize": self.filesize,
            "status": self.status.value,
            "progress": round(self.progress, 1),
            "speed": self.speed,
            "eta": self.eta,
            "error_message": self.error_message,
            "created_at": self.created_at,
        }
```

---

## 二、downloader.py — yt-dlp 封装层（核心）

### 2.1 设计思路

yt-dlp 有两种使用方式：
- **命令行模式**：`subprocess` 调用 `yt-dlp` CLI → 进度解析麻烦，但进程好控制（pause/resume靠kill）
- **Python API模式**：直接 `import yt_dlp` → 进度回调方便，但暂停/恢复需要特殊处理

**决策：采用 Python API 模式**
- 进度通过 `progress_hooks` 回调获取，实时性好
- 暂停：在回调中设置标志位，让下载线程主动退出
- 恢复：重新创建YoutubeDL实例重新下载（yt-dlp自动检测.part文件续传）

### 2.2 解析功能实现

```python
# services/downloader.py

import yt_dlp
from urllib.parse import urlparse

def parse_video(url: str) -> dict:
    """
    解析视频URL，返回标准化信息
    不下载任何内容，仅提取元数据和格式列表
    """
    ydl_opts = {
        'quiet': True,
        'no_warnings': True,
        'extract_flat': False,     # 完整提取播放列表
    }

    with yt_dlp.YoutubeDL(ydl_opts) as ydl:
        info = ydl.extract_info(url, download=False)
        return normalize_info(info)


def normalize_info(raw_info: dict) -> dict:
    """
    将yt-dlp原始输出转换为前端需要的格式
    """
    formats = []
    if raw_info.get('formats'):
        for f in raw_info['formats']:
            if f.get('vcodec') != 'none' or f.get('acodec') != 'none':
                formats.append({
                    'format_id': f['format_id'],
                    'ext': f.get('ext', ''),
                    'resolution': f.get('resolution', f.get('format_note', '')),
                    'filesize': f.get('filesize') or f.get('filesize_approx', 0),
                    'vcodec': f.get('vcodec', ''),
                    'acodec': f.get('acodec', ''),
                    'tbr': f.get('tbr', 0),
                })

    best = pick_best_format(formats)

    return {
        'title': raw_info.get('title', ''),
        'description': raw_info.get('description', '')[:500],
        'thumbnail': raw_info.get('thumbnail', ''),
        'duration': raw_info.get('duration', 0),
        'platform': get_platform(raw_info.get('webpage_url', raw_info.get('url', ''))),
        'uploader': raw_info.get('uploader', ''),
        'formats': formats,
        'is_playlist': '_type' in raw_info and raw_info['_type'] == 'playlist',
        'playlist_count': raw_info.get('playlist_count') if raw_info.get('_type') == 'playlist' else None,
        'entry_count': raw_info.get('entry_count', len(raw_info.get('entries', []))),
        'entries': [e['url'] for e in raw_info.get('entries', [])] if raw_info.get('entries') else [],
        'best_format': best,
    }


def pick_best_format(formats: list[dict]) -> dict:
    """
    智能选择最佳格式
    策略：
    1. 找MP4容器且包含视频+音频的合并流
    2. 若无合并流，找最高分辨率视频流 + 最高音质音频流
    3. 音频-only场景选最高音质
    """
    video_formats = [f for f in formats if f['vcodec'] not in ('none', '')]
    audio_formats = [f for f in formats if f['acodec'] not in ('none', '')]

    if not video_formats and audio_formats:
        best_audio = max(audio_formats, key=lambda x: x.get('tbr', 0))
        return {'format_id': best_audio['format_id'], 'resolution': 'audio only',
                'ext': best_audio['ext'], 'note': '最佳音质'}

    merged_mp4 = [f for f in formats
                  if f['vcodec'] not in ('none', '')
                  and f['acodec'] not in ('none', '')
                  and f['ext'] == 'mp4']
    if merged_mp4:
        best = max(merged_mp4, key=lambda x: x.get('tbr', 0))
        return {'format_id': best['format_id'], 'resolution': best['resolution'],
                'ext': best['ext'], 'note': '最佳画质合并流'}

    if video_formats and audio_formats:
        best_v = max(video_formats, key=lambda x: x.get('tbr', 0))
        best_a = max(audio_formats, key=lambda x: x.get('tbr', 0))
        return {'format_id': f"{best_v['format_id']}+{best_a['format_id']}",
                'resolution': best_v['resolution'],
                'ext': 'mp4', 'note': '视频+音频合并（需合并）'}

    return {'format_id': '', 'resolution': '', 'ext': '', 'note': '无可选格式'}


def get_platform(url: str) -> str:
    domain = urlparse(url).netloc.lower()
    platform_map = {
        'youtube.com': 'youtube', 'youtu.be': 'youtube',
        'bilibili.com': 'bilibili', 'b23.tv': 'bilibili',
        'douyin.com': 'douyin', 'iesdouyin.com': 'douyin',
        'kuaishou.com': 'kuaishou',
        'twitter.com': 'twitter', 'x.com': 'twitter',
        'instagram.com': 'instagram',
        'tiktok.com': 'tiktok',
    }
    for domain_key, name in platform_map.items():
        if domain_key in domain:
            return name
    return 'unknown'
```

### 2.3 下载功能实现（异步+进度推送）

```python
# services/downloader.py 续

import threading
import asyncio


class DownloadEngine:
    """
    下载引擎：封装yt-dlp的下载操作
    每个Task对应一个DownloadEngine实例
    """

    def __init__(self, task: 'Task', config: dict,
                 on_progress=None, on_complete=None, on_error=None):
        self.task = task
        self.config = config
        self.on_progress = on_progress
        self.on_complete = on_complete
        self.on_error = on_error
        self._cancelled = False
        self._ydl = None

    def build_ydl_opts(self) -> dict:
        """根据task和config构建yt-dlp选项"""
        opts = {
            'quiet': True,
            'no_warnings': True,
            'outtmpl': os.path.join(
                self.config['download_path'],
                '%(title)s.%(ext)s'
            ),
            'merge_output_format': self.task.output_format,
            'progress_hooks': [self._progress_hook],
            'postprocessors': [],  # 后期可加缩略图等后处理
        }

        if self.config.get('speed_limit', 0) > 0:
            opts['ratelimit'] = self.config['speed_limit']

        if self.task.format_id:
            opts['format'] = self.task.format_id

        return opts

    def _progress_hook(self, d: dict):
        """yt-dlp进度回调 —— 这是整个系统的核心事件源"""
        try:
            if d['status'] == 'downloading':
                total = d.get('total_bytes') or d.get('total_bytes_estimate', 1)
                downloaded = d.get('downloaded_bytes', 0)
                self.task.progress = (downloaded / total) * 100
                self.task.speed = d.get('_speed_str', '')
                self.task.eta = d.get('_eta_str', '')

                if self.on_progress:
                    self.on_progress(self.task.to_dict())

            elif d['status'] == 'finished':
                self.task.progress = 100
                self.task.status = TaskStatus.MERGING
                if self.on_progress:
                    self.on_progress(self.task.to_dict())

            elif d['status'] == 'error':
                self.task.error_message = d.get('error', '未知错误')
                self.task.status = TaskStatus.FAILED
                if self.on_error:
                    self.on_error(self.task.to_dict())
        except Exception as e:
            print(f"Progress hook error: {e}")

    def run_in_thread(self):
        """在线程中执行下载（阻塞式），由asyncio调度"""
        try:
            self.task.status = TaskStatus.DOWNLOADING
            self.task.started_at = time.time()
            opts = self.build_ydl_opts()

            with yt_dlp.YoutubeDL(opts) as self._ydl:
                info = self._ydl.extract_info(self.task.url, download=True)
                if info:
                    self.task.title = info.get('title', self.task.title)
                    expected_filename = self._ydl.prepare_filename(info)
                    self.task.filepath = expected_filename
                    if os.path.exists(self.task.filepath):
                        self.task.filesize = os.path.getsize(self.task.filepath)

            self.task.status = TaskStatus.COMPLETED
            self.task.completed_at = time.time()
            if self.on_complete:
                self.on_complete(self.task.to_dict())

        except Exception as e:
            self.task.error_message = str(e)
            self.task.status = TaskStatus.FAILED
            if self.on_error:
                self.on_error(self.task.to_dict())

    def cancel(self):
        """取消下载"""
        self._cancelled = True
        if self._ydl:
            try:
                self._ydl.params['abort'] = True
            except:
                pass
```

### 2.4 在 main.py 中集成下载引擎

```python
# main.py 中的关键部分

import asyncio
import threading

async def start_download(task: Task):
    """
    异步启动下载任务
    使用信号量控制并发数
    使用线程池执行阻塞式的yt-dlp下载
    """
    async with semaphore:
        engine = DownloadEngine(
            task=task,
            config=config,
            on_progress=_on_task_update,
            on_complete=_on_task_update,
            on_error=_on_task_update,
        )
        tasks[task.id].status = TaskStatus.DOWNLOADING

        loop = asyncio.get_event_loop()

        def _run():
            engine.run_in_thread()

        await loop.run_in_executor(None, _run)


def _on_task_update(task_data: dict):
    """任务状态变更回调 → 广播给所有WebSocket客户端"""
    tasks[task_data['id']].__dict__.update({
        k: v for k, v in task_data.items() if k in tasks[task_data['id']].to_dict()
    })
    asyncio.create_task(ws_manager.broadcast(task_data))
```

### 2.5 暂停/恢复机制

```python
# 暂停：终止线程 + 保留.part文件（yt-dlp自动保留）
@app.post("/api/download/{task_id}/pause")
async def pause_download(task_id: str):
    task = tasks.get(task_id)
    if not task or task.status != TaskStatus.DOWNLOADING:
        return {"error": "任务不可暂停"}

    task.status = TaskStatus.PAUSED
    # yt-dlp没有真正的pause API，我们通过标记取消来实现
    # .part文件会保留，下次resume时自动续传
    await ws_manager.broadcast(task.to_dict())
    return {"ok": True}

# 恢复：重新创建下载引擎，从.part文件续传
@app.post("/api/download/{task_id}/resume")
async def resume_download(task_id: str):
    task = tasks.get(task_id)
    if not task or task.status != TaskStatus.PAUSED:
        return {"error": "任务不可恢复"}

    task.error_message = ""  # 清除错误
    asyncio.create_task(start_download(task))  # 重新加入队列
    await ws_manager.broadcast(task.to_dict())
    return {"ok": True}
```

---

## 三、converter.py — 格式转换

### 3.1 实现

```python
# services/converter.py
import subprocess
import json

def convert_file(input_path: str, output_format: str,
                 quality: str = "medium") -> dict:
    """
    使用ffmpeg进行格式转换
    quality: high(原质量) / medium(CRF 23) / low(CRF 30)
    """
    input_ext = input_path.rsplit('.', 1)[-1]
    base_name = input_path.rsplit('.', 1)[0]
    output_path = f"{base_name}.{output_format}"

    quality_map = {
        'high': ['-crf', '18'],
        'medium': ['-crf', '23'],
        'low': ['-crf', '30'],
    }

    cmd = ['ffmpeg', '-y', '-i', input_path]

    if output_format in ('mp4', 'webm', 'mkv'):
        cmd += quality_map.get(quality, quality_map['medium'])
        cmd += ['-c:a', 'aac', '-b:a', '192k']
    elif output_format in ('mp3',):
        cmd += ['-vn', '-ab', '192k', '-ar', '44100']
    elif output_format in ('aac',):
        cmd += ['-vn', '-c:a', 'aac', '-b:a', '256k']
    elif output_format in ('flac',):
        cmd += ['-vn', '-c:a', 'flac']
    elif output_format in ('wav',):
        cmd += ['-vn', '-c:a', 'pcm_s16le']

    cmd.append(output_path)

    process = subprocess.Popen(
        cmd,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        text=True
    )

    _, stderr = process.communicate(timeout=3600)  # 1小时超时

    if process.returncode == 0:
        file_size = os.path.getsize(output_path) if os.path.exists(output_path) else 0
        return {
            "success": True,
            "output_path": output_path,
            "file_size": file_size,
        }
    else:
        return {
            "success": False,
            "error": stderr[-500:] if stderr else "转换失败",
        }
```

---

## 四、API路由完整列表与实现要点

### 4.1 POST /api/parse — 解析URL

```python
@app.post("/api/parse")
async def parse_url(req: ParseRequest):
    try:
        result = parse_video(req.url)
        return result
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

**注意**：解析可能耗时较长（需要请求目标网站），设置超时60秒。

### 4.2 POST /api/download — 创建下载任务

```python
@app.post("/api/download")
async def create_download(req: DownloadRequest):
    task = Task(
        url=req.url,
        format_id=req.format_id or "",
        output_format=req.output_format or config["output_format"],
    )

    tasks[task.id] = task
    asyncio.create_task(start_download(task))

    return {"task_id": task.id, "status": "queued"}
```

### 4.3 GET /api/downloads — 获取所有任务

```python
@app.get("/api/downloads")
async def list_tasks(status: Optional[str] = None):
    result = [t.to_dict() for t in tasks.values()]
    if status:
        result = [t for t in result if t['status'] == status]
    return sorted(result, key=lambda x: x['created_at'], reverse=True)
```

### 4.4 DELETE /api/download/{id} — 取消/删除任务

```python
@app.delete("/api/download/{task_id}")
async def cancel_download(task_id: str):
    task = tasks.pop(task_id, None)
    if not task:
        return {"ok": False}
    # 正在下载的任务会被引擎检测到_cancelled标志后停止
    return {"ok": True}
```

### 4.5 POST /api/batch — 批量下载

```python
@app.post("/api/batch")
async def batch_download(req: BatchRequest):
    urls = req.urls or req.entries or []

    created = []
    for url in urls:
        task = Task(
            url=url,
            format_id=req.format_id or "",
            output_format=req.output_format or config["output_format"],
        )
        tasks[task.id] = task
        asyncio.create_task(start_download(task))
        created.append(task.id)

    return {"batch_id": str(uuid.uuid4())[:8], "total": len(created), "task_ids": created}
```

### 4.6 WS /ws/downloads — WebSocket实时推送

```python
@app.websocket("/ws/downloads")
async def websocket_endpoint(websocket: WebSocket):
    await ws_manager.connect(websocket)
    try:
        # 发送当前所有任务状态（新连接同步）
        current_state = [t.to_dict() for t in tasks.values()]
        await websocket.send_json({"type": "init", "data": current_state})

        while True:
            data = await websocket.receive_text()
            # 可以处理前端发来的消息（如心跳保活）
            if data == "ping":
                await websocket.send_json({"type": "pong"})
    except:
        ws_manager.disconnect(websocket)
```

### 4.7 设置相关

```python
@app.get("/api/settings")
async def get_settings():
    return config

@app.put("/api/settings")
async def update_settings(update: SettingsUpdate):
    config.update(update.model_dump(exclude_unset=True))
    # 如果更新了并发数，重建信号量
    if 'max_concurrent' in update.model_dump():
        global semaphore
        semaphore = asyncio.Semaphore(config["max_concurrent"])
    return config
```

---

## 五、前端架构详解

### 5.1 技术选型与依赖

```json
// package.json 核心依赖
{
  "dependencies": {
    "vue": "^3.4",
    "vue-router": "^4.3",
    "pinia": "^2.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0",
    "vite": "^5.x",
    "tailwindcss": "^3.4",
    "postcss": "^8.4",
    "autoprefixer": "^10.4"
  }
}
```

### 5.2 Pinia Store 设计

```javascript
// stores/download.js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useDownloadStore = defineStore('download', () => {
  // ===== 状态 =====
  const tasks = ref([])           // 所有任务列表
  const wsConnected = ref(false)  // WebSocket连接状态
  const parsedVideo = ref(null)   // 当前解析的视频信息
  const parsing = ref(false)      // 是否正在解析

  let ws = null                   // WebSocket实例

  // ===== 计算属性 =====
  const activeTasks = computed(() =>
    tasks.value.filter(t => ['queued','downloading','merging'].includes(t.status))
  )
  const completedTasks = computed(() =>
    tasks.value.filter(t => t.status === 'completed')
  )
  const failedTasks = computed(() =>
    tasks.value.filter(t => t.status === 'failed')
  )
  const downloadingCount = computed(() => activeTasks.value.length)

  // ===== 方法 =====
  function connectWS() {
    const protocol = location.protocol === 'https:' ? 'wss:' : 'ws:'
    ws = new WebSocket(`${protocol}//${location.host}/ws/downloads`)

    ws.onopen = () => { wsConnected.value = true }
    ws.onclose = () => {
      wsConnected.value = false
      setTimeout(connectWS, 3000)  // 自动重连
    }
    ws.onmessage = (event) => {
      const msg = JSON.parse(event.data)
      if (msg.type === 'init') {
        tasks.value = msg.data
      } else {
        // 单个任务更新
        const idx = tasks.value.findIndex(t => t.id === msg.id)
        if (idx >= 0) {
          tasks.value[idx] = msg
        } else {
          tasks.value.push(msg)
        }
      }
    }
  }

  async function parseUrl(url) {
    parsing.value = true
    parsedVideo.value = null
    const res = await fetch('/api/parse', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ url })
    })
    parsedVideo.value = await res.json()
    parsing.value = false
  }

  async function startDownload(options) {
    const res = await fetch('/api/download', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(options)
    })
    return await res.json()
  }

  async function pauseTask(id) {
    await fetch(`/api/download/${id}/pause`, { method: 'POST' })
  }

  async function resumeTask(id) {
    await fetch(`/api/download/${id}/resume`, { method: 'POST' })
  }

  async function cancelTask(id) {
    await fetch(`/api/download/${id}`, { method: 'DELETE' })
  }

  // 初始化
  connectWS()

  return {
    tasks, wsConnected, parsedVideo, parsing,
    activeTasks, completedTasks, failedTasks, downloadingCount,
    connectWS, parseUrl, startDownload, pauseTask, resumeTask, cancelTask
  }
})
```

### 5.3 组件层级与数据流

```
App.vue
└── AppLayout.vue
    ├── AppHeader.vue          （标题栏）
    ├── AppSidebar.vue         （导航菜单）
    └── <router-view>
        ├── HomeView.vue       ← 主页面
        │   ├── UrlInput.vue        （输入框 → 调用 store.parseUrl()）
        │   ├── VideoPreview.vue    （展示 store.parsedVideo）
        │   │   └── FormatSelector.vue  （格式选择）
        │   └── DownloadQueue.vue   （展示 store.activeTasks）
        │       └── DownloadCard.vue × N  （每个任务卡片）
        └── SettingsView.vue     （设置页）
```

### 5.4 核心组件交互流程

#### UrlInput.vue — URL输入组件

```
用户行为流程：

1. 用户粘贴URL到输入框
2. 按回车 或 点击"解析"按钮
3. → 调用 store.parseUrl(url)
4. → 显示加载动画（parsing=true）
5. → 后端返回解析结果 → store.parsedVideo 更新
6. → VideoPreview.vue 自动显示（v-if="parsedVideo"）
7. → 同时显示格式选择和下载按钮
```

**关键实现细节**：
- 输入框支持 `paste` 事件自动触发解析
- 支持拖拽链接到输入框
- 自动识别剪贴板内容是否为URL
- 错误提示：无效URL、不支持的平台、解析超时

#### VideoPreview.vue — 视频预览卡片

```
展示内容：
┌─────────────────────────────────────┐
│  [封面图]                            │
│                                      │
│  📺 视频标题                          │
│  👤 上传者 · ⏱ 时长 · 🏷 平台标识     │
│                                      │
│  推荐格式：1080p MP4 (合并流)         │
│  ┌─────────────────────────────┐    │
│  │ FormatSelector.vue          │    │
│  └─────────────────────────────┘    │
│                                      │
│  [⬇️ 最佳质量下载]  [🎵 仅音频]       │
└─────────────────────────────────────┘
```

**点击下载按钮时**：
1. 收集用户选择的格式信息
2. 调用 `store.startDownload({ url, format_id, output_format })`
3. 后端返回 `{ task_id }`
4. 任务自动出现在下方下载队列中（WebSocket推送）

#### DownloadCard.vue — 下载任务卡片

```
┌─────────────────────────────────────┐
│  📺 视频标题                    [×]  │
│  ████████████░░░░░ 65%               │
│  5.2MB/s  ·  剩余 02:30             │
│  [⏸ 暂停]  [📂 打开文件]              │
└─────────────────────────────────────┘

状态变化：
- queued:        灰色，显示"排队中..."
- downloading:   蓝色渐变进度条，速度+ETA动态刷新
- paused:        黄色，显示"已暂停"，按钮变为[▶ 恢复]
- merging:       紫色，显示"合并中..."
- completed:     绿色，显示✓完成，按钮[📂打开][📂目录]
- failed:        红色，显示错误信息，按钮[🔄 重试][× 删除]
```

**进度条样式**：
- 使用CSS渐变模拟流动效果
- 百分比数字实时更新
- 速度和剩余时间每秒刷新

### 5.5 Vite代理配置

```javascript
// vite.config.js
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8976',
        changeOrigin: true,
      },
      '/ws': {
        target: 'ws://localhost:8976',
        ws: true,
      }
    }
  }
})
```

这样前端 `fetch('/api/...')` 和 `new WebSocket('/ws/...')` 都能正确转发到后端。

---

## 六、一键启动脚本 start.py

```python
#!/usr/bin/env python3
"""一键启动脚本：检查环境 → 安装依赖 → 启动服务 → 打开浏览器"""

import sys
import subprocess
import os
import webbrowser
import shutil
import time

PORT = 8976

def check_python_version():
    if sys.version_info < (3, 10):
        print("❌ 需要Python 3.10或更高版本")
        sys.exit(1)
    print(f"✅ Python {sys.version.split()[0]}")

def install_deps():
    """安装Python依赖"""
    req_file = os.path.join(os.path.dirname(__file__), 'backend', 'requirements.txt')
    if os.path.exists(req_file):
        print("📦 安装依赖...")
        subprocess.check_call([sys.executable, '-m', 'pip', 'install', '-r', req_file, '-q'])

def check_yt_dlp():
    """检查并安装yt-dlp"""
    try:
        import yt_dlp
        print(f"✅ yt-dlp 已安装")
    except ImportError:
        print("📦 安装 yt-dlp...")
        subprocess.check_call([sys.executable, '-m', 'pip', 'install', 'yt-dlp', '-q'])

def check_ffmpeg():
    """检查ffmpeg是否可用"""
    if shutil.which('ffmpeg'):
        print("✅ ffmpeg 已安装")
        return True
    else:
        print("⚠️  未检测到ffmpeg")
        print("   格式转换功能将不可用")
        print("   请安装: https://ffmpeg.org/download.html")
        return False

def ensure_dirs():
    """确保必要目录存在"""
    os.makedirs('./downloads', exist_ok=True)
    os.makedirs('./frontend/dist', exist_ok=True)

def build_frontend():
    """构建前端（如果dist不存在）"""
    dist_dir = './frontend/dist'
    if not os.listdir(dist_dir) if os.path.exists(dist_dir) else True:
        frontend_dir = os.path.join(os.path.dirname(__file__), 'frontend')
        if os.path.exists(os.path.join(frontend_dir, 'package.json')):
            print("🔨 构建前端...")
            subprocess.run(['npm', 'run', 'build'], cwd=frontend_dir, check=True)

def start_server():
    """启动FastAPI服务器"""
    backend_dir = os.path.join(os.path.dirname(__file__), 'backend')
    sys.path.insert(0, backend_dir)

    import uvicorn
    from fastapi.staticfiles import StaticFiles
    import main

    app = main.app
    dist_dir = os.path.join(os.path.dirname(__file__), 'frontend', 'dist')
    if os.path.isdir(dist_dir):
        app.mount("/", StaticFiles(directory=dist_dir, html=True), name="static")

    print(f"\n🚀 服务启动: http://localhost:{PORT}")
    webbrowser.open(f'http://localhost:{PORT}')

    uvicorn.run(app, host='127.0.0.1', port=PORT)

if __name__ == '__main__':
    print("=" * 50)
    print("  🎬 AI万能视频下载器")
    print("=" * 50)

    check_python_version()
    install_deps()
    check_yt_dlp()
    check_ffmpeg()
    ensure_dirs()
    build_frontend()
    start_server()
```

---

## 七、开发顺序与依赖关系

```
阶段一：基础骨架（按以下顺序编写文件）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1: 创建目录结构
Step 2: backend/requirements.txt
Step 3: backend/services/__init__.py
Step 4: backend/services/downloader.py（先写parse函数，可独立测试）
Step 5: backend/main.py（FastAPI骨架 + /api/hello 测试接口）
Step 6: 前端初始化（npm create vite@latest → 安装依赖 → 配置TailwindCSS）
Step 7: frontend/src/stores/download.js（Pinia store）
Step 8: 前端联调测试（首页显示hello接口数据）


阶段二：核心功能
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 9: 完善 downloader.py（下载引擎 + 进度回调）
Step 10: main.py 补全所有API路由（parse/download/tasks/pause/resume/delete）
Step 11: main.py 实现 WebSocket 端点
Step 12: backend/services/converter.py（格式转换）
Step 13: HomeView.vue — URL输入 + 解析结果展示
Step 14: VideoPreview.vue + FormatSelector.vue
Step 15: DownloadCard.vue + 下载队列展示
Step 16: 端到端测试：粘贴URL → 解析 → 选择格式 → 下载 → 看进度


阶段三：完善与打磨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 17: AppLayout.vue + AppSidebar.vue + AppHeader.vue
Step 18: SettingsView.vue
Step 19: 批量下载功能
Step 20: 错误处理优化（网络错误、解析失败、磁盘空间不足等）
Step 21: UI动效（进度条动画、状态切换过渡）
Step 22: start.py 一键启动脚本
Step 23: 全面测试验收
```

---

## 八、关键技术点说明

### 8.1 yt-dlp 进度回调的数据结构

yt-dlp 的 progress_hooks 回调中，`d` 字典的关键字段：

| status值 | 含义 | 可用字段 |
|----------|------|----------|
| `downloading` | 正在下载 | downloaded_bytes, total_bytes, total_bytes_estimate, speed, eta, _speed_str, _eta_str |
| `finished` | 下载完成，待合并 | filename, total_bytes |
| `error` | 出错 | error |
| `processing` | 后处理（如合并） | |

**注意**：`total_bytes` 可能一开始为None，需要用 `total_bytes_estimate` 作为备选。

### 8.2 WebSocket 断线重连策略

```
前端连接建立 → 接收 init 消息（全量任务状态同步）
       ↓
正常接收增量更新
       ↓
连接断开（关闭浏览器/网络波动）
       ↓
onclose 触发 → 3秒后自动 reconnect
       ↓
新连接建立 → 再次接收 init 消息 → 状态完全恢复
```

### 8.3 并发下载的实现原理

```python
# 关键：asyncio.Semaphore + run_in_executor

semaphore = asyncio.Semaphore(3)  # 最多同时3个下载

async def start_download(task):
    async with semaphore:          # 获取信号量，超过3个则等待
        await loop.run_in_executor(  # 在线程池中执行阻塞下载
            None, engine.run_in_thread
        )
```

效果：
- 用户快速添加10个视频 → 前3个立即开始下载
- 任意一个完成后 → 第4个自动开始
- 无需手动管理队列顺序

### 8.4 静态文件托管（生产模式）

start.py 中将前端 `dist/` 目录挂载为静态文件：
```python
app.mount("/", StaticFiles(directory="frontend/dist", html=True), name="static")
```
这样生产模式下只需访问一个端口，无需单独启动前端dev server。

### 8.5 CORS 与安全

MVP本地应用，CORS全开放：
```python
app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])
```

---

## 九、文件清单与预估代码量

| 文件 | 行数预估 | 说明 |
|------|----------|------|
| backend/requirements.txt | 5 | 依赖列表 |
| backend/services/__init__.py | 1 | 空文件 |
| backend/services/downloader.py | ~200 | 解析+下载引擎 |
| backend/services/converter.py | ~80 | ffmpeg封装 |
| backend/main.py | ~300 | FastAPI全部路由+任务管理 |
| start.py | ~80 | 一键启动脚本 |
| frontend/package.json | 20 | 前端依赖 |
| vite.config.js | 25 | Vite+代理配置 |
| tailwind.config.js | 15 | Tailwind主题定制 |
| frontend/src/main.js | 5 | 入口 |
| frontend/src/App.vue | 15 | 根组件 |
| frontend/src/router/index.js | 10 | 路由 |
| frontend/src/stores/download.js | ~120 | Pinia状态管理 |
| frontend/src/views/HomeView.vue | ~150 | 首页主视图 |
| frontend/src/views/SettingsView.vue | ~80 | 设置页 |
| frontend/src/components/layout/AppSidebar.vue | ~40 | 侧边栏 |
| frontend/src/components/layout/AppHeader.vue | ~20 | 顶栏 |
| frontend/src/components/download/UrlInput.vue | ~80 | URL输入 |
| frontend/src/components/download/VideoPreview.vue | ~100 | 视频预览 |
| frontend/src/components/download/FormatSelector.vue | ~70 | 格式选择 |
| frontend/src/components/download/DownloadCard.vue | ~120 | 任务卡片 |
| frontend/src/assets/styles/main.css | 30 | 自定义样式 |
| index.html | 15 | HTML模板 |
| postcss.config.js | 10 | PostCSS配置 |
| **合计** | **~1580行** | |

---

## 十、风险点与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| yt-dlp解析超时 | 用户体验差 | 设置60s超时 + 加载提示 |
| 某些平台反爬导致失败 | 功能不可用 | 友好错误提示 + 建议--update |
| 大文件下载内存占用高 | 内存溢出 | yt-dlp流式下载，不占额外内存 |
| ffmpeg未安装 | 转换不可用 | 启动时检测 + 提示安装路径 |
| 中文文件名乱码 | 文件保存异常 | yt-dlp内部已处理Unicode |
| 并发过多被封IP | 下载失败 | 默认限制3并发 + 可调低 |
